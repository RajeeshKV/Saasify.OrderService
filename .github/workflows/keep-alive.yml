name: Keep Render App Active

on:
  schedule:
    - cron: '*/7 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
    - name: Ping Render App
      run: |
        curl -f -s -o /dev/null -w "%{http_code}" https://saasifyapi.rajeesh.online/api/health || exit 1
        echo "App pinged successfully at $(date)"
